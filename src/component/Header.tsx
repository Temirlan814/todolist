"use client";

import React from "react";
import "../Css-Modules/Header.css"; // Импортируем стили

const Header: React.FC = () => {
    return <header className="header">To-Do List</header>;
};

export default Header;
